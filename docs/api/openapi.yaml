openapi: "3.1.0"
info:
  title: Bank Statement Extraction API
  description: |
    AI-powered PDF/Excel → structured ledger data pipeline.
    Full interactive spec available at `/docs` (Swagger UI) and `/redoc` (ReDoc)
    when the API is running.
  version: "0.1.0"
  contact:
    name: BSE Engineering
servers:
  - url: http://localhost:8000
    description: Local development
  - url: https://api.yourdomain.com
    description: Production

# Full spec is auto-generated by FastAPI at /openapi.json
# Snapshot this file periodically with:
#   curl http://localhost:8000/openapi.json -o docs/api/openapi.json
paths: {}
